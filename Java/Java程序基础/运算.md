## 整数运算

Java整数运算遵循四则运算规则
### 溢出
### 自增自减
### 移位运算
### 位运算
### 运算优先级
### 类型自动提升与强制转型
运算过程中，如果参与计算的两个数类型不一致，那么计算结果为较大类型的整型。例如，`short` 和 `int` 计算时，结果总是 `int` ，原因是 `short` 首先自动被转型为 `int` 

也可以将结果强制转型，即将大范围的整数转型为小范围的整数。强制转型使用 `(类型)` 。

## 浮点数运算
### 类型提升
### 溢出
整数运算在除数为 `0` 时会报错，而浮点数运算在除数为0时不会报错，会返回几个特殊值：
-  `NaN` 表示 Not a Number
- `Infinity` 表示无穷大
- `-Infinity` 表示负无穷大
### 强制转型
浮点数强转整数小数部分会直接丢掉，若要四舍五入加上0.5即可

## 布尔运算

运算符
### 短路运算
### 三元运算符

## 字符和字符串
字符和字符串为两种类型
### 字符类型

### 字符串类型
转义字符
### 字符串连接
可以使用 + 连接任意字符串和其他数据类型，例如
```Java
// 字符串连接
public class Main {
    public static void main(String[] args) {
        String s1 = "Hello";
        String s2 = "world";
        String s = s1 + " " + s2 + "!";
        System.out.println(s); // Hello world!
    }
}

```
如果使用 + 连接字符串和其他数据类型，会将其他数据类型先自动转型为字符串，再连接
```Java
// 字符串连接
public class Main {
    public static void main(String[] args) {
        int age = 25;
        String s = "age is " + age;
        System.out.println(s); // age is 25
    }
}

```
