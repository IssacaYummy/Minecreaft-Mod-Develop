### 输出
`System.out.println` : `println` 是 print line 的缩写，表示输出并换行，如果不想换行，可以使用 `print`

### 格式化输出
和c语言类似
### 输入

```Java
import  java.util.Scanner;  
  
public class Input {  
    public static void main(String[] args) {  
        Scanner Input = new Scanner(System.in);  
        System.out.print("Enter your name: ");  
        String name = Input.nextLine();  
        System.out.print("Enter your age: ");  
        int age = Input.nextInt();  
        System.out.println("Hello, " + name + ". You are " + age + " years old.");  
        Input.close();  
    }  
}
```
首先通过`import`导入`java.util.Scanner`，`import`是导入某个类的语句，必须放到java源代码开头

然后创建 `Input` 对象并传入 `System.in` 。 `System.out` 表示标准输出流，而 `System.in` 表示标准输入流。可以直接使用 `System.in` 读取用户输入，但代码更复杂，可以使用 `Scanner` 简化后续代码。

有了`Scanner`对象后，要读取用户输入的字符串，使用`scanner.nextLine()`，要读取用户输入的整数，使用`scanner.nextInt()`。`Scanner`会自动转换数据类型，因此不必手动转换。